# Resumen del día — LongVivIA
**23 de julio de 2026** (sesión larga, hasta 03:35 AM)

---

## Avances del día — confirmados con evidencia real

### Pilares de derivación

- **AFP / Previsión financiera** (commits `d6a6d34`→`6f946b5`): campo `prevision_afp` en `profiles`, selector "Mi AFP" en dashboard con consentimiento explícito, VIVIAN deriva sin comparar AFPs ni recomendar fondos.
- **Comunidad** (commits `e8d2578`, `da82276`): página `/comunidad`, 6 comunas curadas (Las Condes, Providencia, Santiago, Ñuñoa, San Miguel, Quinta Normal), SENAMA como complemento. Confirmado: solo directorio de derivación, sin interacción usuario-a-usuario.
- **Bienestar activo — biblioteca de videos** (commit `cac53d6`): 7 videos aprobados manualmente por Ariel antes de publicar (Yoga en silla ×2, Tai Chi ×2, Musculatura ×3).

### Fixes técnicos

- **Historial VIVIAN** (commits `68c1833`, `90b211a`, `c240b44`): muestra todas las conversaciones ordenadas por fecha, expansión por grupo, eliminación individual y "borrar todo", bug de timezone corregido (UTC explícito).
- **Juegos nuevos "Entrena tu mente"** (commit `87f5de9`): reemplazo de Memoria/Sopa de letras por 4 juegos con progresión de dificultad (Caza objetos, Secuencia, Diferencias, Test de colores) — confirmado por Ariel, incluyendo guardado de puntaje en Supabase.
- **PWA** — confirmada instalada y funcional vía capturas de pantalla de Ariel (app en launcher Android, notificaciones push administradas, cache activo). Bug de ícono de marca quedó identificado, sin resolver todavía a esta fecha.

### Seguridad / privacidad — alerta histórica cerrada

- **RLS `chat_messages` DELETE**: confirmado `auth.uid() = user_id` — verificado con captura de Supabase.
- **RLS `profiles`**: confirmado visualmente por Ariel en el panel de Supabase — `USING` y `WITH CHECK` con `auth.uid() = id`.
- **Regla de consentimiento conversacional** en `lib/vivian-prompt.ts` (commit `d6a6d34`): confirmada vía diff — VIVIAN pregunta antes de persistir un dato de previsión o AFP mencionado casualmente en el chat, regla espejada para ambos dominios.
- Con esto, la alerta de privacidad abierta desde el 22-07 queda **cerrada por completo**.

### Documentación

- `CLAUDE.md` actualizado y aplicado al repo (commit `b4e3cb3`), consolidando decisiones de estrategia del 22-23/07, estado real de cada pilar y checklist de auditoría.

---

## Pendientes activos al cierre del día

- **Farmacias** — único pilar de producto del árbol original sin implementar. Existe solo como placeholder (`activo: false`, `href: "#"`) en el landing.
- **Bug de ícono PWA** — el ícono del launcher no coincide con el logo de marca. *(Nota: esto se cerró al día siguiente, 24-07 — ver resumen de esa fecha cuando se consolide.)*
- **Legal/operacional** sin cambios este día: SII (pausado en domicilio/usufructo), revisión legal de Términos/Privacidad, Twilio producción (falta verificación Meta Business Manager), registro INAPI, primer aliado B2B (Conecta Mayor UC, sin respuesta).

---

## Inconsistencias o hallazgos resueltos en la auditoría

- El commit `dadc44b` ("7 correcciones") generó duda porque el brief original tenía 11 puntos — verificado vía código que los 11 están cubiertos; la discrepancia numérica se debía a puntos ya resueltos en otros commits.
- **Studio 54** (fiesta retro, pilar Ocio): el brief original tenía la ubicación equivocada ("Santiago Oriente"); el commit `d0b122b` con "Viña del Mar" es la corrección correcta, confirmado vía `scripts/eventos-ocio.sql` y URL de Passline.
- Confirmado que Farmacias quedó fuera del commit `b5834cf` de pilares — no es un olvido de documentación, es un gap real de implementación.

---

## Alertas

Ninguna alerta de seguridad/privacidad quedó abierta al cierre de este día — la única activa (previsión de salud/AFP sin consentimiento) se cerró con evidencia verificada en las tres capas (RLS `chat_messages`, RLS `profiles`, regla conversacional).
